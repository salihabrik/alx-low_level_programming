#include "main.h"

/**
 * _puts - prints a string, followed by a new line, to stdout.
 * @str :string parametr to print
 *
 * Return: Always 0.
 */
void _puts(char *str);
{
    char *str;//  str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    
    while (str[i] != '\0')
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}

